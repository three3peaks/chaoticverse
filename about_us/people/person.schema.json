{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://chaoticverse.local/schemas/person.schema.json",
  "title": "About page person",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "image", "alt", "link", "en", "ru"],
  "properties": {
    "id": {
      "type": "string",
        "description": "Stable id (matches gallery[].authorId in artworks/artworks.json where applicable)."
    },
    "image": {
      "type": "string",
      "description": "Filename in images/people/ (shared across roles; move person in JSON only)."
    },
    "alt": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": ["all", "comic", "orig", "extra"],
      "description": "Artist specialization for About Us columns: all (All-round), comic (Illustrators), orig (Original), extra (Specific). Used on artists.json only."
    },
    "link": {
      "type": "string",
      "description": "Shared external URL (https://…). Used for all languages unless overridden in en.link or ru.link."
    },
    "en": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name; may include HTML such as <br>."
        },
        "link": {
          "type": "string",
          "description": "Optional English-only URL; falls back to top-level link."
        }
      }
    },
    "ru": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string"
        },
        "link": {
          "type": "string",
          "description": "Optional Russian-only URL; falls back to top-level link."
        }
      }
    }
  }
}
